Did a fresh sweep with open-source eyes — the code itself is clean now, but the repo has pre-publication smells:

- assets/ is a workshop, not a gallery. Six intermediate icon files (new_icon*.png/svg variants, the stale unfilled trace, a mystery image.png from day one) sit beside the real masters. A stranger cloning the repo can't tell icon.svg (build input) from new_icon__dark_filled.png (scratch). Keep: the two .xcf masters, the three final SVGs, the generated png/ico/header. Cull or subfolder the rest.
- Cargo.toml has no identity. Missing description, license, repository, authors — the first fields any developer (and SignPath's reviewer, incidentally) looks at. Two minutes.
- BUILD-GUIDE.md now lies. Its "status" section predates the theme system, borderless mode, and the refactor — it still says Segment 4 needs a human checkpoint. For a repo where that guide is part of the charm, stale status reads as abandonment. Needs a refresh or an honest "historical document, see git log" banner.
- README undersells the app. It describes v0.1.0 — no mention of themes, Sakura, borderless mode, or the live style editing. Since it's the landing page SignPath will glance at, worth a features refresh.
- One code nit, honestly minor: AppError::Config moonlights for non-config failures (the settings-window launch error maps into it). A rename to something broader or one more variant would be more truthful — but it's cosmetic, and I'd let it ride rather than churn the error enum before signing.
- Not smells, for the record: the mtime stat per closure-invocation (measured-irrelevant, consciously accepted), the seed config opening settings on first run (deliberate UX), the two dev scripts.

Nothing here blocks SignPath — and the CI workflow that application requires is the natural next piece of work anyway. My suggested order: I do the quick hygiene sweep (assets, Cargo metadata, README/guide refresh — maybe 20 minutes), then we build the CI release workflow, then you file the application. Want the sweep?